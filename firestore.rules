rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Local emulator rules for this checkout.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
